name: CI

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  python:
    if: github.repository != 'arthexis/ci-base'
    uses: arthexis/ci-base/.github/workflows/consumer-ci.yml@v1
